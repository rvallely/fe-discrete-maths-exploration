import Header from './Header';

const Home = () => {
    return (
        <div>
            <Header />
            <p>Welcome to Discrete Maths exploration. Explore the Nav for options.</p>
        </div>
    ) 
}

export default Home;